# Case 009：无结构化 JSON

模拟模型只返回自然语言分析，没有任何 JSON。应返回 `not_found`。

这个岗位整体看起来偏中后台，但 JD 里对后端协作要求较多。建议低成本沟通，不要投入太多准备时间。你可以先问清楚前端占比、团队规模、是否要求独立承担 Node 服务端，以及是否有明确的 Vue3/TypeScript 技术栈。

我没有提供结构化 JSON，因为这里故意模拟模型没有按协议输出。